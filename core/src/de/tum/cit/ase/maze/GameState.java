package de.tum.cit.ase.maze;

//Used this enum both in the player class and gameScreen to handle cutscene events.
/**
 Used this enum in the player class to handle cutscene events and methods.
 */
public enum GameState {
        NORMAL,
        CUTSCENE,
        VICTORY,
        GAME_OVER
}
